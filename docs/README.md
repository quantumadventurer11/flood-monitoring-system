# Documentation

This folder keeps supporting materials out of the project root so the source tree is easier to scan.

## Contents

- `research/` - uploaded or inherited research documents that informed the flood monitoring app.

For the complete repository map, see the root `.readme` file. For setup and demo commands, see the root `README.md`.
